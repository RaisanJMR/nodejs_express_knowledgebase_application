extends layout

block content
        h1 #{heading}
        ul.list-group
            each article, i in articles
             li.list-group-item
                a.link-info(href="/articles/"+article._id)= article.title